mixin navbar-brand (text='', href='#', classes='')
    a.navbar-brand(href=`${href}` class=`${classes}`)= text

mixin navbar(classes='navbar-dark bg-dark')
    nav.navbar(class=`${classes}`)
        .container-fluid
            if block
                block